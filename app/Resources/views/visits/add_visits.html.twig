{% extends 'base_menu.html.twig' %}

{% block content_main %}
    <div class="typo-grid">
        <form id="form_new_visits" url="{{ url('visits-save') }}" type="post">
            <div class="typo-1">
                <div class="grid_3 grid_4">
                    <div class="mail-toolbar">
                        <div class="float-left">
                            <h2>Visit Form</h2>
                        </div>
                        <div class="float-right">
                            <div class="btn-group">
                                <button type="submit" class="btn btn-success">Add New</button>
                            </div>
                            <div class="btn-group">
                                <a href="{{ url('visits-list') }}" type="button" class="btn btn-default">Cancel</a>
                            </div>
                        </div>
                    </div>
                </div>
                {% include 'visits/form_visits.html.twig' %}
            </div>
        </form>
    </div>
{% endblock %}

{% block stylesheets %}  
    <link rel="stylesheet" type="text/css" href="{{ asset('css/add_visits.css', 'visits') }}" />
{% endblock %}

{% block javascripts %}
    <script type="text/javascript" src="{{ asset('js/add_visits.js', 'visits') }}"></script>
{% endblock %}
