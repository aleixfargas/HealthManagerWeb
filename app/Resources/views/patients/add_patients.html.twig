{% extends 'base_menu.html.twig' %}

{% block content_main %}
    <div class="typo-grid">
        <form id="form_new_patient" url="{{ url('patients-save') }}" type="post">
            <div class="typo-1">
                <div class="grid_3 grid_4">
                    <div class="mail-toolbar">
                        <div class="float-right">
                            <div class="btn-group">
                                <button type="submit" class="btn btn-success">Create</button>
                            </div>
                            <div class="btn-group">
                                <a href="{{ url('patients-list') }}" class="btn btn-default">Cancel</a>
                            </div>
                        </div>
                        <div class="float-left">
                            <h2>New Patient Form</h2>
                        </div>
                    </div>
                    <div class="clearfix"></div>
                </div>
                {% include 'patients/form_patients.html.twig' %}
            </div>
        </form>
    </div>
{% endblock %}

{% block stylesheets %}  
    <link rel="stylesheet" type="text/css" href="{{ asset('css/add_patients.css', 'patients') }}" />
{% endblock %}

{% block javascripts %}
    <script type="text/javascript" src="{{ asset('js/add_patients.js', 'patients') }}"></script>
{% endblock %}
